<UserControl x:Class="FishShopApplication.View.UserControls.ProductCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FishShopApplication.View.UserControls"
             mc:Ignorable="d" 
                d:DesignHeight="200" d:DesignWidth="700" 
             FontFamily="Times New Roman" Background="White">

    <Border BorderThickness="1" BorderBrush="Black" Padding="10" Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160"/>
                <ColumnDefinition Width="370"/>
                <ColumnDefinition Width="130"/>
            </Grid.ColumnDefinitions>

            <!-- Фото -->
            <Border BorderThickness="1" BorderBrush="Black" Padding="10">
                <Image Source="{Binding FullPhotoPath}" Stretch="UniformToFill" MaxWidth="90" MaxHeight="90"
                       HorizontalAlignment="Center" VerticalAlignment="Top"/>
            </Border>

            <!-- Основная информация -->
            <Border BorderThickness="1" BorderBrush="Black" Padding="10" Grid.Column="1" Margin="5,0,0,0">
                <StackPanel Margin="10,0,0,0">
                    <TextBlock FontWeight="Bold" FontSize="14" TextWrapping="Wrap">
                        <Run Text="{Binding Category}"/> <Run Text=" | "/> <Run Text="{Binding Name}"/>
                    </TextBlock>
                    <TextBlock Text="{Binding Description, StringFormat='Описание товара: {0}'}" FontSize="12" TextWrapping="Wrap" Margin="0,3,0,0"/>
                    <TextBlock Text="{Binding Manufacturer, StringFormat='Производитель: {0}'}" FontSize="12" Margin="0,2,0,0"/>
                    <TextBlock Text="{Binding Provider, StringFormat='Поставщик: {0}'}" FontSize="12" Margin="0,2,0,0"/>
                    <TextBlock Text="{Binding Price, StringFormat='Цена: {0:C}'}" FontSize="13" FontWeight="SemiBold" Foreground="DarkGreen" Margin="0,5,0,0"/>
                    <TextBlock Text="{Binding Count, StringFormat='Количество на складе: {0}'}" FontSize="11" Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <!-- Скидка -->
            <Border BorderThickness="1" BorderBrush="Black" Padding="10" Grid.Column="2" Margin="5,0,0,0">
                <TextBlock Text="{Binding Discount, StringFormat='Действующая скидка: {0}%'}"
                           TextWrapping="Wrap" HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
